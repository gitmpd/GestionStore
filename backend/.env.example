# Base de données PostgreSQL (optionnel - seulement pour la synchronisation)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gestionstore?schema=public"

# Clé secrète JWT (remplacer par une chaîne aléatoire longue)
JWT_SECRET="change-this-to-a-long-random-string"
JWT_REFRESH_SECRET="change-this-refresh-secret-too"

# Port du serveur
PORT=3001
